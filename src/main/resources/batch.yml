spring:
  batch:
    job:
      enabled: true
      name: healthCheckJob
    jdbc:
      initialize-schema: always

batch:
  schedules:
    monthly-usage-reset:
      enabled: ${BATCH_MONTHLY_USAGE_RESET_ENABLED:true}
      cron: ${BATCH_MONTHLY_USAGE_RESET_CRON:0 1 0 1 * *}
  jobs:
    monthly-usage-reset:
      lock-ttl: ${BATCH_MONTHLY_USAGE_RESET_LOCK_TTL:PT1H}
      redis-chunk-size: ${BATCH_MONTHLY_USAGE_RESET_REDIS_CHUNK_SIZE:1000}
      db-fetch-size: ${BATCH_MONTHLY_USAGE_RESET_DB_FETCH_SIZE:1000}
